<!DOCTYPE html>
<html>
<head>
  <title>Biblio</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href="//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.2/css/bootstrap-combined.min.css" rel="stylesheet">
</head>
<body>
	<div class="container">
		<div class="navbar" style="margin-top:10px">
		  <div class="navbar-inner">
		    <a class="brand" href="<%= root_path %>">Biblio!</a>
		    <ul class="nav">
		      <li><%= link_to 'Home', root_path %></li>
		      		<%# user is logged in, show log out link %>
				<% if current_user.present? %>
				  <li><%= link_to 'My Library', books_path %></li>
				  <li><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %></li>
				<%# user is not logged in, show signup and login links %>
				<% else %>
				  <li><%= link_to 'Sign In', new_user_session_path %> </li>
				  <li><%= link_to 'Register Now!', new_user_registration_path %></li>
				<% end %>
		    </ul>
		  </div>
		</div>

		<%= yield %>
	</div>

</body>
</html>
